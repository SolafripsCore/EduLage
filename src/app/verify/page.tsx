import { SearchCheck } from "lucide-react";
import Image from "next/image";
import { PageIntro } from "@/components/PageIntro";
import { Container } from "@/components/ui/Container";

export const metadata = { title: "Verify a credential", description: "Credential verification on EduLage will launch with the first credential-issuing cohort." };
export default function VerifyPage() {
  return <><PageIntro eyebrow="Trust and records" title="Verify an EduLage-hosted credential" description="Use a credential ID to check a record issued by a participating institution." /><section className="section-space"><Container><div className="grid min-w-0 gap-8 md:grid-cols-[0.8fr_1.2fr] md:items-center"><div className="group relative min-w-0 min-h-[300px] overflow-hidden rounded-xl"><Image src="/media/credential.jpg" alt="Academic credential document" fill sizes="(max-width: 767px) 100vw, 40vw" className="image-zoom object-cover" /><div className="image-scrim absolute inset-0" /><p className="absolute bottom-5 left-5 text-lg font-bold text-white">Records issued by institutions.</p></div><div className="card-hover min-w-0 rounded-xl border border-line p-8 text-center"><SearchCheck size={38} className="mx-auto text-teal-600" strokeWidth={1.5} /><h2 className="mt-5 text-2xl font-bold text-navy-800">Verification is coming soon</h2><p className="mt-3 leading-7 text-ink-600">Credential verification launches with the first credential-issuing cohort. No credentials are available to verify in this preview build.</p><form className="mt-7 flex gap-2"><label htmlFor="verify-id" className="sr-only">Enter credential ID</label><input id="verify-id" name="id" placeholder="Enter credential ID" className="min-w-0 flex-1 rounded-md border border-line px-4 py-3 text-sm focus:border-teal-500 focus:ring-2 focus:ring-teal-500/20" /><button className="rounded-md bg-navy-800 px-5 py-3 text-sm font-semibold text-white hover:bg-navy-700 focus-visible:ring-2 focus-visible:ring-teal-500 focus-visible:ring-offset-2">Verify</button></form></div></div></Container></section></>;
}
