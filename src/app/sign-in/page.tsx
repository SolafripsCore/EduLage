import Link from "next/link";
import { Building2, GraduationCap, MapPin, ShieldCheck } from "lucide-react";
import { PageIntro } from "@/components/PageIntro";
import { Container } from "@/components/ui/Container";
import { learnLinks } from "@/lib/site";
export const metadata={title:"Sign in",description:"Sign in to My learning, the institution and trainer studio, or Open Education Center services."};
const portals=[[GraduationCap,"Learners","My learning","Continue your enrolled programmes and courses, track progress and access learner support.",learnLinks.signIn,"Sign in to My learning"],[Building2,"Institutions and trainers","EduLage Studio","Author and manage authorised programmes, course runs and teaching teams for your institution.",learnLinks.studio,"Open EduLage Studio"],[MapPin,"Open Education Centers","OEC operator services","Center teams manage facilities, supervised examinations and standards records with the EduLage OEC team.","/contact","Contact the OEC team"]] as const;
export default function Page(){return <><PageIntro eyebrow="Sign in" title="One EduLage account for learning, teaching and support." description="Use the same EduLage sign-in wherever you are in the ecosystem. Choose where you want to go."/><section className="py-16 md:py-20"><Container><div className="grid gap-4 md:grid-cols-3">{portals.map(([Icon,eyebrow,title,text,href,action])=><article key={title} className="card-hover flex flex-col rounded-2xl border border-line bg-white p-7"><Icon size={23} className="text-teal-700"/><p className="mt-5 text-xs font-bold uppercase tracking-[0.12em] text-teal-600">{eyebrow}</p><h2 className="mt-2 text-lg font-bold text-navy-800">{title}</h2><p className="mt-3 flex-1 text-sm leading-6 text-ink-600">{text}</p><a href={href} className="mt-6 inline-flex items-center justify-center rounded-md bg-navy-800 px-4 py-2.5 text-sm font-semibold text-white hover:bg-navy-700 focus-visible:ring-2 focus-visible:ring-teal-500 focus-visible:ring-offset-2">{action}</a></article>)}</div><div className="mt-8 flex items-start gap-4 rounded-2xl bg-surface p-7"><ShieldCheck className="shrink-0 text-teal-700"/><div><h2 className="font-bold text-navy-800">New to EduLage?</h2><p className="mt-2 text-sm leading-6 text-ink-600">Learner accounts are created when a participating institution approves your admission—there is no separate registration. Browse programmes and apply through the institution’s admissions process; once approved, sign in here to start learning.</p><div className="mt-4 flex flex-wrap gap-4 text-sm font-bold text-teal-700"><Link href="/programmes">Explore programmes →</Link><Link href="/help">Help &amp; support →</Link></div></div></div></Container></section></>}
