"use client";

import { useMemo, useState } from "react";
import { useSearchParams } from "next/navigation";
import { getProgrammes } from "@/lib/catalog";
import { disciplines } from "@/data/disciplines";
import type { Credential, DeliveryMode, StudyLevel, StudyMode } from "@/data/types";
import { institutions } from "@/data/institutions";
import { ProgrammeCard } from "./ProgrammeCard";

const credentials: Credential[] = ["BSc", "BEng", "LLB", "MSc", "MBA", "MPH", "PhD", "PGD", "Professional Certificate"];
const levels: StudyLevel[] = ["Undergraduate", "Postgraduate", "Doctoral", "Professional"];
const modes: DeliveryMode[] = ["Fully online", "Online + OEC exams"];
const studyModes: StudyMode[] = ["Full-time", "Part-time"];
const countries = [...new Set(institutions.map(item => item.country))].sort();
const languages = ["English", "French", "Mandarin"];
type FilterValues = { discipline: string; credential: string; level: string; mode: string; studyMode: string; country: string; language: string; institutionId: string; query: string };

export function ProgrammesBrowser() {
  const searchParams = useSearchParams();
  const paramString = searchParams.toString();
  const urlValues: FilterValues = { discipline: searchParams.get("discipline") ?? "", credential: searchParams.get("credential") ?? "", level: searchParams.get("level") ?? "", mode: searchParams.get("mode") ?? searchParams.get("deliveryMode") ?? "", studyMode: searchParams.get("studyMode") ?? "", country: searchParams.get("country") ?? "", language: searchParams.get("language") ?? "", institutionId: searchParams.get("institution") ?? "", query: searchParams.get("q") ?? searchParams.get("query") ?? "" };
  const [localValues, setLocalValues] = useState<{ key: string; values: FilterValues } | null>(null);
  const filters = localValues?.key === paramString ? localValues.values : urlValues;
  const updateFilter = (name: keyof FilterValues, value: string) => setLocalValues({ key: paramString, values: { ...filters, [name]: value } });
  const [sort, setSort] = useState("relevance");
  const results = useMemo(() => {
    const items = getProgrammes({ discipline: filters.discipline || undefined, credential: filters.credential as Credential || undefined, level: filters.level as StudyLevel || undefined, deliveryMode: filters.mode as DeliveryMode || undefined, studyMode: filters.studyMode as StudyMode || undefined, country: filters.country || undefined, language: filters.language || undefined, institutionId: filters.institutionId || undefined, query: filters.query || undefined });
    return [...items].sort((a,b) => sort === "tuition-low" ? a.tuitionFrom-b.tuitionFrom : sort === "duration" ? a.durationMonths-b.durationMonths : sort === "intake" ? a.nextIntake.localeCompare(b.nextIntake) : Number(Boolean(b.trending))-Number(Boolean(a.trending)));
  }, [filters, sort]);
  return <div className="grid gap-8 lg:grid-cols-[240px_1fr]">
    <aside className="card-hover rounded-xl border border-line bg-white p-5 lg:h-fit"><div className="flex items-center justify-between"><h2 className="font-bold text-navy-800">Filter programmes</h2><button className="rounded-sm text-xs text-teal-600 focus-visible:ring-2 focus-visible:ring-teal-500" onClick={() => setLocalValues({ key: paramString, values: { discipline: "", credential: "", level: "", mode: "", studyMode: "", country: "", language: "", institutionId: "", query: "" } })}>Clear all</button></div><label className="mt-6 block text-xs font-semibold text-ink-600" htmlFor="programme-query">Search</label><input id="programme-query" value={filters.query} onChange={(event) => updateFilter("query", event.target.value)} placeholder="Programme or subject" className="mt-2 min-h-11 w-full rounded-md border border-line px-3 py-2 text-sm focus:border-teal-500 focus:ring-2 focus:ring-teal-500/20" /><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="institution-filter">Institution</label><select id="institution-filter" value={filters.institutionId} onChange={(event) => updateFilter("institutionId", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All institutions</option>{institutions.map(item=><option key={item.id} value={item.id}>{item.shortName}</option>)}</select><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="discipline-filter">Discipline</label><select id="discipline-filter" value={filters.discipline} onChange={(event) => updateFilter("discipline", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All disciplines</option>{disciplines.map((item) => <option key={item.name}>{item.name}</option>)}</select><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="level-filter">Academic level</label><select id="level-filter" value={filters.level} onChange={(event) => updateFilter("level", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All levels</option>{levels.map((item) => <option key={item}>{item}</option>)}</select><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="credential-filter">Credential</label><select id="credential-filter" value={filters.credential} onChange={(event) => updateFilter("credential", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All credentials</option>{credentials.map((item) => <option key={item}>{item}</option>)}</select><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="country-filter">Country</label><select id="country-filter" value={filters.country} onChange={(event) => updateFilter("country", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All countries</option>{countries.map(item=><option key={item}>{item}</option>)}</select><div className="mt-5 grid grid-cols-2 gap-3"><label className="block text-xs font-semibold text-ink-600">Schedule<select value={filters.studyMode} onChange={(event) => updateFilter("studyMode", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-2 text-xs"><option value="">Any</option>{studyModes.map(item=><option key={item}>{item}</option>)}</select></label><label className="block text-xs font-semibold text-ink-600">Language<select value={filters.language} onChange={(event) => updateFilter("language", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-2 text-xs"><option value="">Any</option>{languages.map(item=><option key={item}>{item}</option>)}</select></label></div><label className="mt-5 block text-xs font-semibold text-ink-600" htmlFor="mode-filter">Delivery mode</label><select id="mode-filter" value={filters.mode} onChange={(event) => updateFilter("mode", event.target.value)} className="mt-2 min-h-11 w-full rounded-md border border-line bg-white px-3 py-2 text-sm"><option value="">All modes</option>{modes.map((item) => <option key={item}>{item}</option>)}</select></aside>
    <div><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-ink-600"><strong className="tabular-nums text-navy-800">{results.length}</strong> {results.length === 1 ? "programme" : "programmes"}</p><label className="flex items-center gap-2 text-sm text-ink-600" htmlFor="sort">Sort by<select id="sort" value={sort} onChange={event=>setSort(event.target.value)} className="rounded-md border border-line bg-white px-2 py-1.5 text-xs focus:border-teal-500 focus:ring-2 focus:ring-teal-500/20"><option value="relevance">Recommended</option><option value="tuition-low">Tuition: low to high</option><option value="duration">Shortest duration</option><option value="intake">Next intake</option></select></label></div>{results.length ? <div className="grid gap-4 md:grid-cols-2">{results.map((programme, index) => <ProgrammeCard key={programme.id} programme={programme} priority={index === 0} />)}</div> : <div className="card-hover rounded-xl border border-line p-10 text-center"><h2 className="font-bold text-navy-800">No programmes match these filters</h2><p className="mt-2 text-sm text-ink-600">Try a different combination or clear all filters.</p></div>}</div>
  </div>;
}
